#include<stdio.h>
int main(){
    int x , y;
    printf("Enter x and y : ");
    scanf("%d%d",&x,&y);
    float temp_r = x*x + y*y;
    float r = sqrt(temp_r);
    float area = 3.14 * r * r;
    float permi = 2 * 

}
